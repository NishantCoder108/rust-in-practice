### Why does `rust-in-practice` repo exist?

> Purpose : To mastery Rust - for writing secure `Solana Smart Contract` and write `Backend Server` more optimized way.

#### Some Points:

> 1. Every directory is source of learning.
> 2. `rektoff`,`rust-by-example`, `rustlings`, rust `book`, `the-dev-method`, are source of learnings.
> 3. `projects` directory is for projects that I built and will continuously add more projects inside at.
